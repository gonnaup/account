<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.gonnaup.accountmanagement.dao.RoleDao">

    <resultMap type="org.gonnaup.accountmanagement.entity.Role" id="RoleMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="applicationName" column="application_name" jdbcType="VARCHAR"/>
        <result property="roleName" column="role_name" jdbcType="VARCHAR"/>
        <result property="description" column="description" jdbcType="VARCHAR"/>
        <result property="createtime" column="createtime" jdbcType="TIMESTAMP"/>
        <result property="updatetime" column="updatetime" jdbcType="TIMESTAMP"/>
        <collection property="permissions" ofType="org.gonnaup.accountmanagement.entity.Permission" column="id"
                    select="org.gonnaup.accountmanagement.dao.PermissionDao.queryByRoleId"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="RoleMap">
        select
          id, application_name, role_name, description, createtime, updatetime
        from role
        where id = #{id}
    </select>

    <select id="queryByAccountId" resultMap="RoleMap">
        select id, application_name, role_name, description, createtime, updatetime
        from role
        join account_role ar on role.id = ar.role_id
        and ar.account_id = #{accountId}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAllConditionalByLimit" resultMap="RoleMap">
        select
        id, application_name, role_name, description, createtime, updatetime
        from role
        <where>
            <if test="role.id != null">
                and id = #{role.id}
            </if>
            <if test="role.applicationName != null and role.applicationName != ''">
                and application_name = #{role.applicationName}
            </if>
            <if test="role.roleName != null and role.roleName != ''">
                and role_name like concat('%', #{role.roleName}, '%')
            </if>
            <if test="role.description != null and role.description != ''">
                and description like concat('%', #{role.description}, '%')
            </if>
        </where>
        limit #{offset}, #{limit}
    </select>

    <!--新增所有列-->
    <insert id="insert">
        insert into role(id, application_name, role_name, description)
        values (#{id}, #{applicationName}, #{roleName}, #{description})
    </insert>

    <insert id="insertBatch">
        insert into role(id, application_name, role_name, description)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.applicationName}, #{entity.roleName}, #{entity.description})
        </foreach>
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update role
        <set>
            <if test="applicationName != null and applicationName != ''">
                application_name = #{applicationName},
            </if>
            <if test="roleName != null and roleName != ''">
                role_name = #{roleName},
            </if>
            <if test="description != null and description != ''">
                description = #{description},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from role where id = #{id} limit 1
    </delete>

</mapper>