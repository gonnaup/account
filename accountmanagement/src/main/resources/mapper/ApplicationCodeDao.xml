<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.gonnaup.accountmanagement.dao.ApplicationCodeDao">

    <resultMap type="org.gonnaup.accountmanagement.entity.ApplicationCode" id="ApplicationCodeMap">
        <result property="applicationName" column="application_name" jdbcType="VARCHAR"/>
        <result property="applicationCode" column="application_code" jdbcType="INTEGER"/>
        <result property="createtime" column="createtime" jdbcType="TIMESTAMP"/>
        <result property="updatetime" column="updatetime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="ApplicationCodeMap">
        select
          application_name, application_code, createtime, updatetime
        from application_code
        where application_name = #{applicationName}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="ApplicationCodeMap">
        select
          application_name, application_code, createtime, updatetime
        from application_code
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="ApplicationCodeMap">
        select
        application_name, application_code, createtime, updatetime
        from application_code
        <where>
            <if test="applicationName != null and applicationName != ''">
                and application_name = #{applicationName}
            </if>
            <if test="applicationCode != null">
                and application_code = #{applicationCode}
            </if>
            <if test="createtime != null">
                and createtime = #{createtime}
            </if>
            <if test="updatetime != null">
                and updatetime = #{updatetime}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="applicationName" useGeneratedKeys="true">
        insert into application_code(application_code, createtime, updatetime)
        values (#{applicationCode}, #{createtime}, #{updatetime})
    </insert>

    <insert id="insertBatch" keyProperty="applicationName" useGeneratedKeys="true">
        insert into application_code(application_code, createtime, updatetime)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.applicationCode}, #{entity.createtime}, #{entity.updatetime})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="applicationName" useGeneratedKeys="true">
        insert into application_code(application_code, createtime, updatetime)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.applicationCode}, #{entity.createtime}, #{entity.updatetime})
        </foreach>
        on duplicate key update
        application_code = values(application_code) , createtime = values(createtime) , updatetime = values(updatetime)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update application_code
        <set>
            <if test="applicationCode != null">
                application_code = #{applicationCode},
            </if>
            <if test="createtime != null">
                createtime = #{createtime},
            </if>
            <if test="updatetime != null">
                updatetime = #{updatetime},
            </if>
        </set>
        where application_name = #{applicationName}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from application_code where application_name = #{applicationName}
    </delete>

</mapper>