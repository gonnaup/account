<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.gonnaup.accountmanagement.dao.ApplicationRpcAccountDao">

    <resultMap type="org.gonnaup.accountmanagement.entity.ApplicationRpcAccount" id="ApplicationRpcAccountMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="applicationName" column="application_name" jdbcType="VARCHAR"/>
        <result property="identifier" column="identifier" jdbcType="VARCHAR"/>
        <result property="createtime" column="createtime" jdbcType="TIMESTAMP"/>
        <result property="updatetime" column="updatetime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="ApplicationRpcAccountMap">
        select
          id, application_name, identifier, createtime, updatetime
        from application_rpc_account
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="ApplicationRpcAccountMap">
        select
          id, application_name, identifier, createtime, updatetime
        from application_rpc_account
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAllConditionalByLimit" resultMap="ApplicationRpcAccountMap">
        select
        id, application_name, identifier, createtime, updatetime
        from application_rpc_account
        <where>
            <if test="applicationRpcAccount.id != null">
                and id = #{applicationRpcAccount.id}
            </if>
            <if test="applicationRpcAccount.applicationName != null and applicationRpcAccount.applicationName != ''">
                and application_name = #{applicationRpcAccount.applicationName}
            </if>
            <if test="applicationRpcAccount.identifier != null and applicationRpcAccount.identifier != ''">
                and identifier = #{applicationRpcAccount.identifier}
            </if>
            <if test="applicationRpcAccount.createtime != null">
                and createtime = #{applicationRpcAccount.createtime}
            </if>
            <if test="applicationRpcAccount.updatetime != null">
                and updatetime = #{applicationRpcAccount.updatetime}
            </if>
        </where>
        limit #{offset}, #{limit}
    </select>

    <select id="queryByApplicationName" resultMap="ApplicationRpcAccountMap">
        select id, application_name, identifier, createtime, updatetime
        from application_rpc_account
        where application_name = #{applicationName}
    </select>

    <!--新增所有列-->
    <insert id="insert">
        insert into application_rpc_account(id, application_name, identifier, createtime, updatetime)
        values (#{id}, #{applicationName}, #{identifier}, #{createtime}, #{updatetime})
    </insert>

    <insert id="insertBatch">
        insert into application_rpc_account(id, application_name, identifier, createtime, updatetime)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.id}, #{entity.applicationName}, #{entity.identifier}, #{entity.createtime}, #{entity.updatetime})
        </foreach>
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update application_rpc_account
        <set>
            <if test="applicationName != null and applicationName != ''">
                application_name = #{applicationName},
            </if>
            <if test="identifier != null and identifier != ''">
                identifier = #{identifier},
            </if>
            <if test="createtime != null">
                createtime = #{createtime},
            </if>
            <if test="updatetime != null">
                updatetime = #{updatetime},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from application_rpc_account where id = #{id}
    </delete>

</mapper>